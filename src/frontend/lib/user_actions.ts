'use server'

// import database variables from env

export const signIn = async ({ username, password }: signInProps) => {
    return 5
}

export const signUp = async ({ password, ...userData }: signUpProps) => {
    return
}